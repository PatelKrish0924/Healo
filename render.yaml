services:
  - type: web
    name: healo-hospital
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn healo_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0